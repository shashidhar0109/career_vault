# (Optional) Database Connection URL
# Career Vault works completely offline and stores data in your browser's localStorage by default.
# If you want to connect a cloud database (e.g. Supabase / PostgreSQL), specify your connection URL below:
DATABASE_URL="your-database-connection-string-here"
